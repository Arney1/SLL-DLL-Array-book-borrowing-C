#ifndef convention_H
#define convention_H
#include <stdbool.h>
// typedef int infotype;
typedef enum { INTEGER, STRING, USER, STOK, BOOK, ELEMEN, BOOKQUEUE } DataType;
#define MAX_STRING_LENGTH 100

#endif
